/-
General-covering slice of the AlgebraicGeometry audit. Despite the audit's
historical name, `EnumDecls` routes the repository's `Topology` root to this
audit. MO1.12 (#1323) moved `isCoveringMap_prodMap_id` out of the
`GL+(2,R)`-cover coordinate argument to the topology owner, because its
statement quantifies over three arbitrary topological spaces. The declaration
name is unchanged; only the lane that owns it.
-/
import DerivedAlgGeo.Topology.Covering.Basic

/-! ## Products of covering maps -/

#print axioms CategoryTheory.Triangulated.WeakStabilityCondition.StabilityCondition.GroupAction.isCoveringMap_prodMap_id
