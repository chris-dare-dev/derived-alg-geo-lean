# Moduli/Stacks -> StabilityCondition/Families migration exceptions.
# Format: source path relative to DerivedAlgGeo, TAB, exact imported module.
#
# The cutover to AlgebraicGeometry/DerivedCategory burned this list to zero.
# Do not add an exception: reusable moduli and stack roots must import the
# neutral owner rather than a stability-specific leaf.
