# Tasks

## 1. ROU1 comparison and dimension core

- [ ] 1.1 Complete #918, comparing the owner `ExtensionClosure` with iterated extension products and `triangEnvelope`. Frozen files: `DerivedAlgGeo/CategoryTheory/Triangulated/Dimension/ExtensionClosureComparison.lean`, `DerivedAlgGeo/CategoryTheory/Triangulated/Dimension.lean`, and `scripts/StabilityConditionAudit/Dimension.lean`. Acceptance: prove every iterated extension product lies in `ExtensionClosure`; add the typeclass-vocabulary `le_of_closed` corollary and document any weakening from its raw hypotheses; prove the envelope inclusion with `P.Nonempty` and triangulated hypotheses; record the `P = ⊥` counterexample; prove the supremum equality only if both inclusions close, otherwise document the exact unproved direction; add the generation-time bound; leave `ExtensionClosure.lean` unchanged. Verify with `LEAN_NUM_THREADS=2 ~/.elan/bin/lake build DerivedAlgGeo.CategoryTheory.Triangulated.Dimension.ExtensionClosureComparison StabilityConditionAudit`, `scripts/precheck.sh`, `~/.elan/bin/lake env lean scripts/StabilityConditionAudit.lean > /tmp/rou1-stability-audit.txt 2>&1 && python3 scripts/check_audit.py /tmp/rou1-stability-audit.txt`, then require the protected `ci` check on the PR.

## 2. Rouquier dimension

- [ ] 2.1 Complete #919 after #918 so the shared umbrella and audit changes remain serial. Frozen files: `DerivedAlgGeo/CategoryTheory/Triangulated/Dimension/Rouquier.lean`, `DerivedAlgGeo/CategoryTheory/Triangulated/Dimension.lean`, and `scripts/StabilityConditionAudit/Dimension.lean`. Acceptance: define the infimum over single objects; state that the value quantifies over single objects without selecting a minimizing witness; state the `⟨G⟩_{n+1}` convention and zero offset; prove the finite-bound, finite-dimension, classical-generator, and zero-dimension characterisations; make no equivalence-invariance claim in this file; add every public declaration to the audit. Verify with `LEAN_NUM_THREADS=2 ~/.elan/bin/lake build DerivedAlgGeo.CategoryTheory.Triangulated.Dimension.Rouquier StabilityConditionAudit`, `scripts/precheck.sh`, `~/.elan/bin/lake env lean scripts/StabilityConditionAudit.lean > /tmp/rou1-stability-audit.txt 2>&1 && python3 scripts/check_audit.py /tmp/rou1-stability-audit.txt`, then require the protected `ci` check on the PR.

## 3. Functorial transport

- [ ] 3.1 Complete #921 after #919. Frozen files: `DerivedAlgGeo/CategoryTheory/Triangulated/Dimension/Functor.lean`, `DerivedAlgGeo/CategoryTheory/Triangulated/Dimension.lean`, and `scripts/StabilityConditionAudit/Dimension.lean`. Acceptance: prove forward pointwise transport and the `ObjectProperty.map` corollary; prove generation-time monotonicity, Rouquier-dimension monotonicity for essentially surjective triangulated functors, and invariance under triangulated equivalence, including transfer of strong generation; document that the reverse transport claim is false in general; audit every public declaration. Verify with `LEAN_NUM_THREADS=2 ~/.elan/bin/lake build DerivedAlgGeo.CategoryTheory.Triangulated.Dimension.Functor StabilityConditionAudit`, `scripts/precheck.sh`, `~/.elan/bin/lake env lean scripts/StabilityConditionAudit.lean > /tmp/rou1-stability-audit.txt 2>&1 && python3 scripts/check_audit.py /tmp/rou1-stability-audit.txt`, then require the protected `ci` check on the PR.
