# Agent observations

- 2026-09-23: Live PR #1494's Actions run 35946740804 and check suite 97338890265 report head `5d708af764a558b76a5f088a01b91cba67d09819`, while the PR merge commit is `a87b031366f3e09adc633860fbfbda839eee44c6`. The run's artifact list names `trust-artifacts-a87b031366f3e09adc633860fbfbda839eee44c6`; its Git parents are the current base and head. The checked-in protected workflow contains `name: trust-artifacts-${{ github.sha }}` and default `actions/checkout`. This resolves the candidate-binding design without a workflow edit, conditional on an unchanged trusted workflow and a present run artifact.
- Task 1.1: Injected-transport pagination/error client implemented; `python3 -m unittest discover -s scripts/tests -p 'test_ci_github_evidence.py' -v` passed 4 tests.
- Tasks 1.2–3.1: Provider identity, protected policy, complete observations, attempt mapping, evidence bundle and documentation implemented. Focused collector tests pass 14/14 and existing contract tests pass 38/38. `git diff --check` and Python compilation pass.
- Local `scripts/precheck.sh` passed workflows, output-encoding, loop-engine-tests, local-build, mathlib-style, explicit-numerical-data, foundation-import-boundary, nolints-ratchet, pin, source-independence, subject-layering, umbrella-coverage, root-reachability, coherent-families, coverage-map, gates-agreement and targeted-build. It failed `loop-spec` because existing `.claude/loop-specs/issue-1394-audit-record-cleanup.yaml` has `base_ref` other than `origin/main`, and `roadmap` because live issue #1495 is a missing milestone entry/blocker mismatch. Neither failure is in this change's file scope. Task 3.2 remains open pending a clean precheck on updated main or an explicit record of baseline failure at PR review.
- First frozen commit `619c5dbb99666a1e1299600e76557339abf70d72` received four independent reviews. Source and boundary reviewers rejected stale required-CI claims: a newer distinct run cannot supersede an older red run on one head, and a new attempt can begin between gate enumeration and return. Boundary review also found that duplicate protected `checks` entries were collapsed. Abstraction review identified the same freshness gap and a real failed-jobs-only rerun whose artifact predates attempt 2; style review requested readable multiline evidence records, sorted imports, and assertions for each paginated surface. All findings were retained; no round was counted as acceptance.
- Round 1 fixes: require a single distinct CI run for the PR head across events, recheck direct run identity and attempt after collection, reject duplicate protection entries, document mixed-attempt rerun denial, format Python with Black, and assert retained suite/check/status IDs. Focused collector tests now pass 16/16; contract tests pass 38/38. The frozen follow-up commit still needs exact-revision review.
- Second frozen commit `ab6639f6f895906f2580f131c661d32e04a8095e` received all four independent reviews. Boundary, abstraction and style accepted. Source-faithfulness found that contradictory workflow/job/check conclusions could still produce a required-CI claim. The follow-up rejects a job/check disagreement and denies a non-successful primary workflow conclusion, with regression fixtures. This finding is resolved in code but requires review on the expanded final commit.
- The issue scope was extended to a read-only `loop_engine.py evidence` command for a live collector → schema v4 validator → controller demonstration. A runless `github-advanced-security` check now uses its actual PR-head subject and check-app provider identity; it has no invented Actions run. Retained artifacts from earlier attempts are preserved as observations while current-attempt selection remains conservative. Durable queue admission, workflow routing and merge authority remain unchanged for #1434.
- Current focused suites: collector 19/19, schema contract 39/39 and controller 82/82 passed. OpenSpec strict validation, `git diff --check` and Black checks for the new collector files passed. The latest `scripts/precheck.sh` passed workflows, output-encoding, loop-spec, loop-engine-tests, local-build, mathlib-style, explicit-numerical-data, foundation-import-boundary, nolints-ratchet, pin, source-independence, subject-layering, umbrella-coverage, root-reachability, coherent-families, coverage-map, gates-agreement and targeted-build. `roadmap` failed on pre-existing live issue/roadmap mismatches for #1495 and #1496 (RM-02, RM-05, RM-06). No file in this change owns those entries. The full precheck cannot be recorded as passing.
- A read-only controller exercise against open PR #1497 denied a claim because its PR base differed from current protected main. It performed no provider mutation and produced no substitute candidate. A current-revision exercise remains pending on this change's own PR.
- Merged current protected main `0762be6753935808f0c944d5d623db201926d4f9` from roadmap PR #1498 into the branch; the implementation diff remained unchanged. After that merge, all 19 named `scripts/precheck.sh` gates passed: workflows, output-encoding, loop-spec, loop-engine-tests, local-build, mathlib-style, explicit-numerical-data, foundation-import-boundary, nolints-ratchet, pin, source-independence, subject-layering, umbrella-coverage, root-reachability, coherent-families, coverage-map, roadmap, gates-agreement and targeted-build. This is a local precheck result, not a hosted CI verdict.
- Expanded-scope review on merge commit `ff9a33566eea63a22d83796c226d445355d7ff18`: source-faithfulness and repository-boundary reviewers accepted. Style review identified an inaccurate candidate/head sentence in the contract and two missing denial fixtures. These are corrected in the follow-up: check/status enumeration is documented as PR-head data with a separately proven merge candidate; the controller normal-denial and duplicate security check-app paths have focused tests. Current suites pass collector 20/20, contract 39/39 and controller 83/83; local precheck remains clean. The final follow-up commit needs exact-head review.
- Four independent lenses accepted PR #1500 head `0e1cf66483435aa428786a20a112bd17e8e98b66`: source-faithfulness, repository boundary, abstraction/adoption and style. A later contract-version audit found that the new `check` binding had been added under the published version 4, so this acceptance is superseded for the schema change. The new implementation uses version 5 for the binding, still reads protected version 4, emits evidence in the trusted inventory's version, and rejects mixed versions or version 4 `check` entries. Focused migration fixtures cover a version 4 base and preserved candidate-only artifact subject. This follow-up requires fresh exact-head review and hosted CI after publication.
- Migration commit `231b5b474a977e47b97fa63a2dcbc0db205febce` was accepted by source-faithfulness, repository-boundary and abstraction reviewers. Style review found candidate/head wording still stale in the OpenSpec artifacts and requested a version 4 fixture with a real green check-app observation. The follow-up corrects the wording and verifies that the old protected policy keeps that check as an observation without granting auxiliary health. The focused suites pass 40 contract, 21 collector and 83 controller tests; all 19 local precheck gates pass. Exact-head style re-review and hosted CI remain pending.
- PR #1500 exact head `f66d7a070f2ef75a67ab803cfded69b8f5d6ea1f` received four independent accepting reviews on that SHA: source-faithfulness checked the provider/base/candidate claims; repository-boundary checked guarded paths, protected-base policy, and unchanged queue/merge code; abstraction/adoption checked canonical validator ownership and v4/v5 separation; style checked OpenSpec, documentation and fixtures. Earlier style findings on PR-head wording, v4 security-check mapping and denied-verdict tests were resolved before this review. The no-conflict base refreshes to `237c396de418876341fcf351eadc1f2abe3b0ff2` and `3664d465e1c263bbc8a6591ee4762f752c6db918` left all 14 planned-file diffs byte-identical; each lens accepted the exact refreshed SHA. These are technical reviews, not human workflow/controller owner signoffs or a `trust-reviewed` attestation.
- [PR #1500](https://github.com/chris-dare-dev/derived-alg-geo-lean/pull/1500) required [hosted CI run 35958808919](https://github.com/chris-dare-dev/derived-alg-geo-lean/actions/runs/35958808919), attempt 1, completed successfully for head `f66d7a070f2ef75a67ab803cfded69b8f5d6ea1f`: `build`, `roadmap` and aggregate `ci` all passed. No self-hosted manual dispatch was used. The focused suites pass 40 contract, 21 collector and 83 controller tests (144 total); all 19 named local precheck gates pass. The PR remains open and unmerged.
- The real read-only `loop_engine.py evidence` command against that PR produced schema 4 evidence from current protected base `3664d465e1c263bbc8a6591ee4762f752c6db918`, PR head `f66d7a070f2ef75a67ab803cfded69b8f5d6ea1f`, tested merge candidate `564b1e67b24a302f57efa0737f0f61e4bd096c65`, candidate tree `3803b13e233cb4f174d0deecccca7693b43aa2cc`, run 35958808919 attempt 1. Validator: `valid=true`, `required_ci_verified=true`, no errors, `auxiliary_checks_healthy=false` and `all_pipelines_green=false` because the optional security check is missing under the protected v4 inventory. All three referenced artifact SHA-256 digests match retained bytes. An earlier same-head call while the run was incomplete exited 1 with `required_ci_verified=false` and no candidate. The command performed only provider GETs and an optional local bundle write; the PR remained open on the same base/head without a new label, comment or review. Exact evidence is in the PR description and [issue comment](https://github.com/chris-dare-dev/derived-alg-geo-lean/issues/1430#issuecomment-5808344106).
- The issue/contract require independent workflow-owner and controller-owner signoffs before adoption and closure. The supplied AGENTS snapshot described a `trust-reviewed` label, but current protected main has retired that label and workflow; `docs/architecture/loop-engineering-friction.md` records the correction. No owner signoff is claimed here. Task 3.6 remains open for normal merge, published revision verification and live evidence.
- After protected main advanced to `e036ec67f001285c9dbdfb24b52eaa9bb22f45d5`, the branch merged that base at `a2950f697ad93f6aadd5f45a10edea364a79e02f`; its 14 planned-file diffs remained byte-identical. A separate repository-boundary review then identified an audit gap: the collector compared live required-check protection with the trusted inventory but omitted the protection API response from the retained bundle. The follow-up retains canonical `live-protection.json` and `protected-inventory.json`, hashes both in the bundle manifest, and checks retained inventory bytes against the evidence policy binding. The focused fixture checks both retained files, manifest hashes and the inventory-tamper denial. Those earlier runs are historical; the current-base record follows.

- Final implementation revision before the tracking update: `a77c35b24a40824f2dac80280c2929a84d3f3e34` on protected base `8683445ddae37dd681ca8f0911d2d5b6c14de5ba`. The no-conflict base refresh preserved all 14 planned-file diffs (three-dot diff SHA-256 `6aaf3826d8cf675a8141bc60841e85dc5f7fc633bab773ae00c924aaa3e4fc32`).
- Four independent technical review lenses accepted that exact revision: source-faithfulness, repository-boundary, abstraction/adoption and mathlib-style. The reviews found no unresolved implementation finding. The abstraction review identified a tracking mismatch (review completion was stated in the PR body while tasks/observations still said pending); this commit reconciles those records. These are technical reviews; independent workflow/controller owner signoffs remain pending.
- On `a77c35b24a40824f2dac80280c2929a84d3f3e34`, focused tests passed: 40 contract, 21 collector and 83 controller (144 total). All 19 named `scripts/precheck.sh` gates passed against protected base `8683445ddae37dd681ca8f0911d2d5b6c14de5ba`; OpenSpec strict validation and `git diff --check` passed. No Lean source or target changed.
- [Hosted PR run 35963832312 attempt 1](https://github.com/chris-dare-dev/derived-alg-geo-lean/actions/runs/35963832312) completed successfully for head `a77c35b24a40824f2dac80280c2929a84d3f3e34`: `roadmap`, `build` and required aggregate `ci` all passed.
- The read-only `scripts/loop_engine.py evidence` command against PR #1500 on that head used base `8683445ddae37dd681ca8f0911d2d5b6c14de5ba`, tested candidate `a45adef45368b24d4d31b7d8ec10330b4978ae93`, candidate tree `6d138e9711558499cfbc131997ff4d6fcfaa6f56`, and run 35963832312 attempt 1. Schema 4 validation returned `valid=true`, `required_ci_verified=true`, no errors; `auxiliary_checks_healthy=false` and `all_pipelines_green=false` because the optional security check is absent under trusted v4 policy. A read during incomplete CI exited 1 with `required_ci_verified=false` and no substituted candidate.
- The retained bundle at `/tmp/ci1-controller-pr1500-final-a77` includes three check-run artifacts, `protected-inventory.json`, `live-protection.json` and source observations; their manifest hashes and byte sizes match retained bytes. SHA-256 values: inventory `512d4d06f0dd93705c4c5961e670b88985cb2ea53b6a719f959a29f6b4a4a8f6`, live protection `215488545a1f5b31b3a126c956334caeb00834cb798f14e0aae56116575169cc`, observations `234095fcd0ce0dfd10620a3bb522198f30036ac8d64dcbfa25c88d93e979ff4e`. Live protection requires strict context `ci` from app ID 15368. The command performed no provider mutation; post-read base, PR head/checks and PR labels/reviews were unchanged.
- Exact evidence was posted as non-closing [issue comment #5809167439](https://github.com/chris-dare-dev/derived-alg-geo-lean/issues/1430#issuecomment-5809167439). The PR and issue remain open for human owner signoffs, normal merge, published-revision verification and closure.
- Protected main advanced to `2e1802037f37606b16b5db8bd8f424ba2efa0e89` after the tracking update was pushed. The controller correctly declined a current-base claim while PR #1500 still used base `8683445ddae37dd681ca8f0911d2d5b6c14de5ba`; run 35966422318 was pending on that stale base. The branch merged current main at `a8f709f2d44a30a7ee3cc7133d1bd60a5c4dfc90`, preserving the planned-file diffs. On refreshed head `3006589de4b8f2b85718f2463d0c2dc021680e7a`, base `2e1802037f37606b16b5db8bd8f424ba2efa0e89`, and run 35966746803, the live controller read denied the incomplete run (`required_ci_verified=false`) without substituting a candidate. Tasks 3.3 and 3.5 remain pending until a successful current-head CI run and completed-run evidence are recorded.
