# Tasks

## 1. Tracking and safe bootstrap

- [x] 1.1 Create the distinct controller tracking issue #1483 with the CI-failure evidence, same-ledger history, and three-round cap; verify `gh issue view 1483 --repo chris-dare-dev/derived-alg-geo-lean --json state,title,body` reports it open and the body excludes PR #1471 implementation edits from scope.
- [x] 1.2 Add `.claude/loop-specs/loop-passed-ci-failure-recovery.yaml` for only issue #1483 and one frozen chunk with `closure: complete`, four required reviewers, a three-round cap, required check `ci`, and every provider mutation flag false; verify `python3 scripts/loop_engine.py validate --spec .claude/loop-specs/loop-passed-ci-failure-recovery.yaml` passes and the validator reports the expected frozen paths and closure mode.
- [ ] 1.3 Strictly validate and independently review the OpenSpec plan plus disabled manifest, then use the one-time owner-authorized plan-only bootstrap specified in this design (an explicit adaptation of exact-state safeguards from `passed-ledger-head-revalidation`, not a procedure already defined there): the PR body says `Refs #1483`, title/commit messages have no closing keyword, provider `closingIssuesReferences` is empty, and only planning files are in its diff. Require current hosted checks green on the exact PR head and reread PR head/base plus issue state immediately before the authorized merge; merge with `gh pr merge --squash --match-head-commit <reviewed-plan-head>` and stop if the head does not match. Verify #1483 remains open afterward. This is not the normal work-PR workflow.
- [ ] 1.4 From a fresh clean worktree at the exact post-bootstrap protected base, validate the disabled manifest, confirm `preflight` reports `DISABLED` after its read-only owner-review source check and without running provider mutations, and initialize the implementation ledger; verify the selected issue, chunk file list, plan digests, provider-disabled policy, and three-round cap match the frozen contract. Separately verify the current base, issue state, and branch/PR collision status through read-only provider checks for the one-time manual publication procedure.

## 2. Exact CI evidence and charged repair transition

- [ ] 2.1 Build one provider snapshot for the matching open non-draft PR, full head SHA, source repository/branch, base, issue links, and live remote head; bind check-run/status SHA plus check name/context and app identity where available to that exact SHA and require the check to be in both the manifest-required and live branch-protection sets. Unlike the existing green-check fallback, fail closed if the live protection set is unavailable or ambiguous. After fetching checks, reread the PR and source ref and reject a moved head. Verify focused tests cover each refusal without changing ledger bytes.
- [ ] 2.2 Tighten generic revalidation so it accepts only the same reviewed-change fingerprint carried across protected-base movement affecting the reviewed scope; reject a changed implementation through that path. For changed implementation, append an immutable event to the existing ledger's `ci_failure_repairs` array and either append the next ordinary `ci_failure_repair` round when a slot remains or record `cap_exhausted` and set `repair_exhausted` when it does not. Keep `rounds` as the only review/cap root and preserve all prior round evidence. Make ledger initialization scan all nested directories under `.loop-runs` by frozen issue/slug/chunk identity, and reject state-directory overrides outside that canonical root. Verify tests cover event shape/immutability, exact-head admission, normal cap counting, repeated failures, terminal exhaustion, and attempts to replace the ledger from another state directory.
- [ ] 2.3 Use the selected ordinary ledger as canonical authority for every shipping path; add required `--ledger` to `push` and `close`, and check manifest digest plus issue/chunk/branch bindings. Apply one common repair-state guard to push, draft/PR creation, ready, attestation, approval, merge, and close before provider mutation. Verify the first draft PR remains possible without a repair event, all shipping paths reject pending/exhausted/mismatched ledgers, a same-head successful CI rerun consumes no slot, and a passing repair still requires current green checks on the exact PR head before merge.
- [ ] 2.4 Update `docs/architecture/loop-recovery.md`, `.claude/loop-specs/README.md`, `.claude/skills/run-loop/SKILL.md`, and `docs/architecture/loop-engineering-friction.md`; verify documentation distinguishes charged CI repair from protected-base revalidation and records the stale-worktree / cap-accounting observation.

## 3. Verification and handoff

- [ ] 3.1 Run `python3 scripts/tests/test_loop_engine.py`, `openspec validate loop-passed-ci-failure-recovery --strict --no-interactive`, `python3 scripts/loop_engine.py validate --spec .claude/loop-specs/loop-passed-ci-failure-recovery.yaml`, `scripts/precheck.sh`, and `git diff --check`; report each result separately and make no Lean-build claim for this Python-only change.
- [ ] 3.2 Obtain independent mathematical/source-faithfulness, repository-boundary, abstraction/adoption, and controller/style reviews on the same exact commit; revise only within the frozen file list and stop after three critique/revise rounds. Verify the complete panel and cap in the ledger.
- [ ] 3.3 Run the hosted pull-request CI on the reviewed controller commit and verify required contexts are green before merge under the separately authorized controller-change procedure. Re-read the exact PR head and merge only with `gh pr merge --squash --match-head-commit <reviewed-head>`; stop if the server rejects that match. Check the exact protected base's active trust-review policy before merging: if it requires `trust-reviewed`, stop for the repository owner to read the guarded diff and add the label; the agent must not apply it. The current base removed that gate in #1449, so do not infer that it is active from stale checkout guidance. Verify no issue is closed unless a matching merged PR explicitly authorizes that closure.
