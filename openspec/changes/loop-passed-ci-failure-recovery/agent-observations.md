# Agent observations

This log records workflow and information-quality issues encountered while planning issue #1483. These observations are not implementation or review evidence.

| Date | Source and observation | Effect | Prevention / disposition |
| --- | --- | --- | --- |
| 2026-09-23 | The original SF8 implementation worktree was still at `edbce012`, while live PR #1471 had advanced to `e39a6c3` by merging `main` at `e18e20f`. The older worktree's controller treated a passed ledger as terminal; the live merged controller already had up to two changed-head revalidation panels, separate from the configured critique/revise cap. | The initial diagnosis said there was no supported changed-head review path. That was inaccurate for the live PR; the actual gap is that generic revalidation does not bind code repair to exact required-check failure and does not charge the ordinary review cap. The issue body was corrected before this plan was drafted. | Compare the live PR head and protected base, fetch the exact PR branch, and inspect its controller/docs before diagnosing a ledger dead end. Keep base-only revalidation distinct from a CI-driven code repair. |
| 2026-09-23 | Adversarial plan review found that the live generic revalidation implementation accepts any different commit after a pass; its `revalidation` label does not prove protected-base movement caused the change. The proposal initially repeated the documentation's narrower description. | A changed implementation could enter an uncharged panel without using CI-failure admission, so the original cap would still be bypassable. | Tighten and test the revalidation classifier against the provider base and reviewed-change fingerprint. Route changed implementation through the charged CI-failure transition only. |
| 2026-09-23 | Adversarial plan review found `action_push` and `action_close` have no ordinary-ledger argument, and `action_create_pr` permits non-blocked in-progress states. The initial spec nevertheless promised that shipping was denied while repair review was pending. | Without a single ledger/chunk binding across these actions, the new repair state could not reliably stop publication or closure. | Make the existing ledger the sole authority, require `--ledger` on push/close, define issue/chunk/branch matching, and require a shared repair-state guard before every relevant provider mutation. Add negative tests per action. |
| 2026-09-23 | Adversarial review found no persisted representation for cap exhaustion, and the initial task said to append failure evidence only when allocating a review slot even though the spec promised to keep exhausted-cap evidence. | A prior `status: passed` could still satisfy merge gates after failure; the task and spec disagreed about whether failure evidence survives exhaustion. | Keep immutable events in the ordinary ledger's `ci_failure_repairs` list, preserve `rounds` as the sole cap root, and set a terminal `repair_exhausted` status when no slot remains. Derive pending/resolved state from the event's linked round; add tests proving no publish path accepts the preserved prior pass. |
| 2026-09-23 | Adversarial review found `ledger init` scans only the requested state directory; a different `--state-dir` can evade the renamed-ledger check and initialize the same frozen work again. | The design's first “no replacement ledger” statement exceeded the existing controller's actual persistence guard. | Keep ordinary ledgers under the existing `.loop-runs` root, scan that root recursively using frozen issue/slug/chunk identity, and reject alternate paths outside it. Add a regression test for sibling nested directories and unchanged terminal-ledger bytes. |
| 2026-09-23 | A read-only adversarial check found PR #1471 had already moved beyond motivating head `e39a6c3`; the current reported head was `9c2381b`, with its `build` check in progress. | Treating the earlier failure as current evidence would violate the planned exact-head admission rule and risk acting on stale CI state. | Use `e39a6c3` only as motivation. At execution time, refresh PR head, source-ref SHA, required-check status and live protection; admit repair only for a completed failure on that exact current SHA. |
| 2026-09-23 | The primary checkout was at `503817d7`, behind `origin/main` at `e18e20f`, and contained unrelated untracked `.claude/worktrees/` and `openspec/changes/controller-ledger-integrity/`. | Planning from the primary checkout could have used stale controller behavior or disturbed user-owned draft material. | Create an isolated worktree at the exact observed `origin/main` commit; leave the primary checkout and its untracked material untouched. |
| 2026-09-23 | `openspec list --specs --json` returned an empty canonical spec inventory, while active/historical OpenSpec deltas already use the in-flight capability path `loop-engineering`; `passed-ledger-head-revalidation` is explicitly superseded by #1473. | Treating the old delta as an active implementation plan or inventing a near-duplicate capability name would have muddied ownership. | Reuse the established `loop-engineering` path for a new delta, and state explicitly that no archived main spec exists. Keep the superseded base-revalidation change unchanged. |
| 2026-09-23 | Repository `openspec/config.yaml` says unattended apply should continue from planning into implementation, while the selected `openspec-propose` skill says its invocation is planning-only and requires stopping after artifacts. | The allowed handoff point is unclear when a loop task creates a new OpenSpec change. This turn follows the selected skill's planning boundary and has not started controller implementation. | Reconcile project context and skill workflow guidance so unattended planning and apply phases have one unambiguous transition rule. |
| 2026-09-23 | Open-issue search found #1475, but its body scopes clean-bootstrap recovery for missing #1451 history and explicitly excludes unrelated controller objectives; no issue matched required-CI-failure repair. | Reusing #1475 would have silently widened another frozen task. | Created distinct issue #1483 and limited its body to exact-head, cap-charged CI repair; keep #1475 unchanged. |
| 2026-09-24 | In a fresh worktree at `237c396d`, the existing controller rejects an enabled manifest whose frozen chunk touches protected controller paths; setting `enabled: false` makes manifest validation pass, while `preflight` correctly returns `DISABLED` and performs no provider checks. | The disabled self-controller manifest cannot use normal preflight as a successful gate before its implementation exists, and enabling it would rely on the very controller policy being changed. | Keep the manifest disabled with every provider mutation false. Treat `DISABLED` as the required no-action result, perform explicit read-only base/issue/branch checks for the one-time owner-authorized bootstrap, and do not claim normal preflight passed. |
| 2026-09-24 | Independent plan review found two provider capabilities (`ready_pr`, `create_issue`) omitted from the disabled manifest and identified that an immediate pre-merge reread alone does not condition the merge on the reviewed PR head. The exact updated base `3664d465` has only `.github/workflows/ci.yml`; commit #1449 removed the trust guard, though stale checkout instructions still describe it as active. | Omitted capability defaults could violate the all-false plan contract; a concurrent head move could race a manual merge; relying on stale trust-gate instructions could either miss a live human gate or invent a retired one. | Explicitly set all eight mutation capabilities false. Use `gh pr merge --match-head-commit <reviewed-head>` on both bootstrap paths. Recheck the exact protected base for a live trust-review gate immediately before controller merge; if active, hand off to the owner for reading/labeling, and never apply the label as agent. |
