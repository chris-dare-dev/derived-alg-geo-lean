# Tasks

## 1. Issue-first loop bootstrap

- [x] 1.1 Use the live issue bodies as the requirements for #137, #182, and #183; keep one shared progress record and remove redundant per-repository plan, email, and re-authorization prerequisites.
- [x] 1.2 Document the issue-only start and completion path in the repository loop guidance.

## 2. Copier template — issue #182

- [x] 2.1 Finish the anchor-optional Copier template, honest generated metadata, full source pin, external-declaration guidance, and false default trust record for newly generated topics.
- [x] 2.2 Verify zero Bridgeland-specific strings, generated-project build, and Copier update from the template commit.

## 3. Second-topic gate — issue #183

- [x] 3.1 Register the `analytic-nt` notebook and pin `1202.3670v4`; fetch, parse, and ingest it without requiring a contact email.
- [x] 3.2 Create five sourced registry entries, including the publisher contents locator and direct exact binding to `Nat.infinite_setOf_prime`.
- [x] 3.3 Refresh the clean environment digest, run the adopter build and MFC contract commands, inspect scope/relation/coverage output, and record dated template and adopter trust evidence.

## 4. Publish and close — issue #137

- [ ] 4.1 Review the final diffs, run the required package checks, and open the repository PR for the MFC template/gate implementation.
- [ ] 4.2 Verify hosted checks and merge when branch protection permits; close #182 and #183 only after the implementation is merged, then close #137.

Each task is complete when its acceptance evidence is recorded below. If a real test, source, permission, or branch-protection failure occurs, fix it or report that specific external limit and keep unrelated work moving.
