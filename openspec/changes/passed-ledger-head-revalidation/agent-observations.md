# Agent Observations

These are workflow/tooling observations, not mathematical evidence. Preserve
the active worktree, exact Git/provider evidence, consequence, and prevention
step. After ledger initialization, keep this file frozen with the OpenSpec
digest; add implementation-time observations to the authorized friction-log
path instead.

| Date | Observation and evidence | Effect | Prevention |
| --- | --- | --- | --- |
| 2026-09-22 | The initial OpenSpec lookup ran from the primary checkout and exposed `dt1-m41`; the actual DT1 #928 change `dt1-928-coherent-tensor` was in the isolated implementation worktree. | Would have attached the controller work to the wrong task/spec if the root had been inferred from conversation context. | Resolve and report the active OpenSpec root with `openspec status --change ... --json` before editing; read paths from that result. |
| 2026-09-22 | During planning, the worktree's base was `d01283f581127a924a148bd442fb19ff3def2a0d`, while `git ls-remote origin refs/heads/main` returned `799c6f9e92c6231861f6878b6e951411247abf6b`; planning artifacts were untracked, not committed. | The first planning checkout was already stale before publication; a bootstrap PR based on it would have repeated the integration hazard. | Check the exact live protected-base OID before creating a branch or publishing; preserve uncommitted artifacts while moving the planning branch to the verified base. |
| 2026-09-22 | The active #928 ledger is legacy-shaped, passed on `7a41ba1ccd2aca21b7919c2a9180161a1a2a9b72`, has two allocated rounds (round 1 needs-changes, round 2 pass), and a three-round cap. Its current branch head is `3e0df103f450c20f888abca3aa3c43bec61a432c`; the ledger remains terminal and was not edited. | Confirmed that the controller needs a bounded event transition rather than reopening/replacing old review evidence. | Test a disposable legacy-schema fixture with an actual Git parent/rebased-candidate graph; never mutate the active DT1 ledger to test migration. |
| 2026-09-22 | An advisor referred to `ledger reconcile-openspec` / `backfill_ledger_locator`, but a search of the active main's `scripts/loop_engine.py` and tests found no such command or helper. The advisor later identified this as context from a different controller-runtime-evidence worktree. | A stale path could have led implementation to depend on a nonexistent compatibility feature. | Search the exact active checkout and require exact file/line evidence for referenced commands before incorporating them into requirements or tests. |
| 2026-09-22 | An advisor asserted normal review/adjudication lacked serialization; inspecting `main()` showed every CLI `dispatch` is wrapped in `controller_lock(root.resolve())`. | Adding a second lock would have been redundant and might have created a false ownership boundary. | Inspect the complete CLI entrypoint and invocation path, not only the individual handler, before diagnosing concurrency. |
| 2026-09-22 | `openspec validate --change` failed because this CLI uses a positional item name; `openspec validate --help` showed the supported syntax. | One validation attempt failed before checking the artifact. This was an inferred CLI mismatch, not an artifact failure. | Probe the specific subcommand's help instead of assuming sibling commands share flags. |
| 2026-09-22 | The existing broad PR inventory uses `--limit 200`, which cannot establish absence of one source-branch PR if pagination is truncated. | A no-PR admission decision based on that list could be false. | Query all states for the exact frozen source branch, verify the returned shape/completeness, and fail closed on truncation or ambiguity. |
| 2026-09-22 | Reviewing current `scripts/loop_engine.py` showed that `action_push` has no ledger parameter and only checks branch/cleanliness, while ordinary `action_create_pr` checks manifest/OpenSpec digests but rejects only `status == blocked`; neither has the new passed-head freshness checks yet. | An enabled manifest with mutation flags set in the planning bootstrap would give the old controller premature provider authority, even though the new spec promises exact-pass gating. | Keep every mutation flag false in the bootstrap manifest and throughout this self-modifying run. Publish the feature only via the documented exact-head bootstrap procedure after the four-role panel and required CI pass; use the new ledger-bound controller actions in subsequent runs. |
| 2026-09-22 | While the bootstrap artifacts were being validated, live `main` advanced from `799c6f9e92c6231861f6878b6e951411247abf6b` to `c4c6118ac3c2669b2275aaeafa0d79256432a46e` via planning PR #1468. The incoming files did not overlap the untracked draft, so the branch was fast-forwarded and both validations were rerun. | Publishing from the initially observed base would already have created a stale planning PR. | Query the protected-base OID immediately before publishing; fast-forward only after inspecting the exact incoming path set, preserve untracked drafts, and rerun OpenSpec and manifest validation afterward. |
| 2026-09-22 | An advisor said any `scripts/` edit requires a human-added `trust-reviewed` label. On the exact live base `c4c6118ac3c2669b2275aaeafa0d79256432a46e`, `.github/workflows/trust-guard.yml` is absent, `ci.yml` is the applicable workflow, and merged controller PR #1450 changed `scripts/loop_engine.py` with only `ci`, `build`, and `roadmap` checks and no labels. The stale primary checkout was 13 commits behind and still contained the retired trust guard. | Following the stale advice would add a nonexistent CI gate and could stop or misreport the bootstrap. | Resolve the exact fetched base before applying workflow advice; distinguish live required checks from retained historical notes, old local worktrees, and untrusted advisor claims. |
| 2026-09-22 | `gh pr diff 1470 --check` was rejected because this installed GitHub CLI has no `--check` flag for `pr diff`; its local help lists only display/filter options. | One remote diff check did not run; it was not a PR or CI failure. The staged commit had already passed local `git diff --cached --check`. | Use `git diff --check` on the frozen local commit before publication, and confirm CLI-specific flags with the subcommand's own help rather than inferring them. |
| 2026-09-22 | The merged planning PR #1470 said `This PR does not ... close #1469`. GitHub treated the negated `close #1469` phrase as a real closing reference and closed the issue on merge. Read-only PR metadata confirmed #1469 as a closing reference; the issue was reopened and the PR body edited, though GitHub retained the historical reference metadata. | The plan-only PR briefly closed the implementation issue before any code was written. | Never put a closing keyword next to a tracked issue in negated prose. Require a standalone canonical `Closes #N` line for complete PRs, non-closing reference syntax for progress, and verify the provider's issue-link metadata before merge. |
| 2026-09-22 | A one-line Python probe initially imported `scripts.loop_engine` as a package and failed because the controller imports its sibling `loop_recovery` as a top-level module; `scripts/` is the import root for the controller. | The first probe did not reach closure validation, which could have been mistaken for a negative test result. | When probing the script module outside its CLI, set `PYTHONPATH=scripts` and distinguish import/setup failures from the behavior under test. |
| 2026-09-22 | Two adversarial reviewers and the current GitHub documentation showed the initial closure-body contract omitted optional-colon and case-insensitive forms, the full nine closing verbs, qualified cross-repository IDs (including same-number foreign issues), source commit messages, and generated squash-merge messages. A checkout search also found no checked-in self-bootstrap procedure despite the design referring to one. | The initial plan could allow unintended issue closure through a syntax variant or merge-message source, and its publication instructions cited an absent authority path. | Freeze title/body/commit/merge-message checks and owner-qualified provider identity in spec/tests; use explicit checked merge subject/body; replace the nonexistent procedure reference with a narrowly scoped, owner-authorized bootstrap checklist, keeping this run's manifest mutation flags false. |
| 2026-09-22 | Final adversarial review caught that an earlier bootstrap sequence placed CI before the push/PR that triggers it, omitted the frozen `squash` flag, did not guard planning-PR commit/squash messages, and did not re-read provider closure references after controller PR creation. | The proposed workflow was either impossible to execute in order or left provider state unchecked at a publication boundary. | Push/create only after exact local review passes, wait for hosted CI, then re-read all bound state before explicit method/head-matched merge; use `--squash`, explicit subject/body, check all plan-PR message sources, and verify post-creation references before success. |
| 2026-09-23 | Before code-ledger initialization, OpenSpec apply instructions required checking completed numbered tasks in `tasks.md`, while `scripts/loop_engine.py:openspec_digest` hashes every required artifact byte-for-byte and review/merge actions reject any digest change. | Following the required task-progress workflow after ledger initialization would invalidate the ledger even though the frozen requirements and task descriptions were unchanged. No implementation source had been edited and no ledger had been initialized when this was found. | Normalize only numbered `N.N` task completion markers in the digest; keep task IDs, wording, order, all other checkboxes, and every other planning artifact content digest-sensitive, with explicit positive and negative regression tests. |
| 2026-09-23 | One bounded adversarial pass found that a global digest rewrite could invalidate legacy ledger/recovery records, a textual regex could normalize task-like examples inside fenced or quoted material, and the first bootstrap-history rewrite omitted the earlier task's exact provider/message acceptance evidence. A second adversary also required the normalized all-unchecked baseline to equal the old raw digest. | Without those boundaries the fix could strand existing runs, hide planning-text edits, or erase unfinished task acceptance evidence while preparing the new ledger. | Version the digest scheme in the frozen manifest (legacy omission remains raw); normalize only actual top-level, non-fenced `tasks.md` task lines to `[ ]`; test raw compatibility and Markdown near-misses; preserve exact bootstrap evidence and verify baseline equivalence before ledger initialization. |
