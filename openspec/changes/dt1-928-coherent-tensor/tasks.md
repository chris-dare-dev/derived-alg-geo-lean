# Tasks

## 1. Freeze the conditional continuation

- [ ] 1.1 Validate the fresh one-issue manifest, live #928 eligibility, and the append-only observation register before ledger initialization; verify with `openspec validate --all --strict --no-interactive`, `python3 scripts/loop_engine.py validate`, and `python3 scripts/loop_engine.py preflight`.
- [ ] 1.2 Implement the generic full-subcategory two-variable shift and exact-bifunctor restriction with all closure and coherence hypotheses explicit; verify with named Lean builds of `CategoryTheory.ObjectProperty.Bifunctor` and `CategoryTheory.Triangulated.ExactFunctorFamily`.
- [ ] 1.3 Add the generic declarations to their existing audit surface and run focused source-independence, layering, umbrella, no-sorry, and targeted audit checks.
- [ ] 1.4 Run the two advisors and four independent adversarial reviewers on the frozen generic commit; record verbatim findings and resolve only recorded findings in at most three rounds.

## 2. Assemble the bounded coherent tensor

- [ ] 2.1 Revalidate the passing generic ledger and append the result to the observation register before freezing the Tensor-local file list and conditional `D(Coh X)` hypotheses; the controller has no same-issue chunk dependency, so do not represent this manual gate as one.
- [ ] 2.2 Add the named fixed-kernel transfer lemmas and the conditional coherent-tensor constructor using the generic bridge and named bounded monoidal restriction; verify with a named Lean build of `AlgebraicGeometry.DerivedCategory.Tensor.CoherentAssembly`.
- [ ] 2.3 Export the new Tensor leaf, audit its public declarations, and correct any affected Tensor-local prose that still describes three independent fields rather than full exact-bifunctor coherence; add neither an instance nor a module-sheaf/coherent-ambient comparison. Verify Tensor umbrella coverage, layering, derived-operations audit, and `scripts/precheck.sh`.
- [ ] 2.4 Run the two advisors and four independent adversarial reviewers on the frozen assembly commit; record verbatim findings and resolve only recorded findings in at most three rounds.

## 3. Guarded handoff and learning record

- [ ] 3.1 Append only verified controller, tracker, documentation, cache, and proof-boundary friction to the change observation register before each affected ledger initialization; verify every entry cites a reproducible command, source, or live tracker fact.
- [ ] 3.2 Use controller actions only for the reviewed PR, required CI, approval, merge, and #928 closure; verify the final ledger digest, frozen files, and merged closing reference match the issue.
