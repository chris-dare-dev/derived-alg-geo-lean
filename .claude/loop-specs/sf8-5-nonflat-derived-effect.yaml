schema: derived-alg-geo-lean.loop-run/v1
id: sf8-5-nonflat-derived-effect
repository: chris-dare-dev/derived-alg-geo-lean
actor: chris-dare-dev
remote: origin
base_branch: main
base_ref: origin/main
mode: independent
enabled: true
allow_force_push: false
state_dir: .loop-runs
roadmap_gate: required

openspec:
  change: sf8-sf9-pilot
  validation: cli-required
  required_artifacts:
    - proposal.md
    - design.md
    - tasks.md
    - specs/sf8-sf9-supported-moduli/spec.md

limits:
  min_issues: 1
  max_issues: 1
  max_review_rounds_per_chunk: 2

review:
  independent: true
  reviewers:
    - mathematics-adversary
    - repository-boundary-adversary
    - abstraction-adversary
    - mathlib-reviewer
  advisors:
    - altitude-scout
    - hypothesis-elimination-scout

runner:
  required_checks:
    - ci

merge:
  method: squash
  delete_branch: false
  allow_method_override: true
  allow_delete_branch_override: true
  allow_auto: true
  allow_admin: true

closure:
  code_issue: pr_merge_keyword
  allow_non_pr: false
  allow_progress_pr: true

mutations:
  comment_issue: true
  close_issue: true
  push_branch: true
  create_pr: true
  approve_pr: true
  merge_pr: true

issues:
  - number: 554
    slug: sf8-5-nonflat-derived-effect
    depends_on: []
    chunks:
      - id: sf8-5-nonflat-derived-effect
        scope: Calculate one explicit nonzero degree-minus-one homology effect for the supported non-flat affine map ℤ → ZMod 2 using a two-term free resolution; do not claim full-locus preservation, a general K-flat existence theorem, or a scheme-level LeftDerivedPullback.
        requirements:
          - Supported arbitrary derived pullbacks are constructed
        files:
          - docs/architecture/generalization-backlog.md
          - DerivedAlgGeo/AlgebraicGeometry/DerivedCategory.lean
          - DerivedAlgGeo/AlgebraicGeometry/DerivedCategory/Dqc/AffineNonflatExample.lean
          - DerivedAlgGeo/AlgebraicGeometry/DerivedCategory/Dqc/AffineNonflatDerivedObject.lean
          - openspec/changes/sf8-sf9-pilot/tasks.md
          - scripts/AlgebraicGeometryAudit/SchemeDerived.lean
        lift_targets:
          - DerivedAlgGeo/Algebra/Homology/DerivedCategory
        acceptance:
          - A two-term bounded-above projective resolution of ZMod 2 is an actual Lean complex with a proved quasi-isomorphism to ZMod 2.
          - Its affine extension of scalars along ℤ → ZMod 2 has a proved nonzero degree-minus-one homology witness, connected to the supported affine bounded-projective representative.
          - The result is explicitly restricted to the affine bounded-projective derived lane and does not claim a general scheme-level LeftDerivedPullback, Dqc preservation, relative-perfect preservation, or an unproved Tor comparison.
        closure: progress
