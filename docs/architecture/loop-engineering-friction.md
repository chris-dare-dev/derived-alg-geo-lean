# Loop-engineering friction log

This log records concrete documentation, checkout, discovery, and proof-probing
failures encountered while progressing the SF8/SF9 loop. It is separate from
the mathematical generalization backlog: an API being hard to discover is not
evidence that a mathematical generalization is valid. Each entry records a
prevention step for later runs.

| Date | Friction and effect | Prevention for later runs |
| --- | --- | --- |
| 2026-09-21 | A search centered on `KProjective` initially suggested that affine bounded-above projective preservation/coherence was missing. The merged base already had it under `BoundedAboveProjective.lean` and `AffineKProjective*` files. | Search filename inventories and mathematical synonyms (`bounded above`, `affine`, `preserves projectives`, `coherence`) before proposing a chunk; verify declarations and audits on the exact base. |
| 2026-09-21 | The root checkout and the isolated SF8 worktree exposed different `generalization-backlog.md` contents. An initial read from the wrong checkout hid the existing UNVERIFIED Tor-comparison row. | Before reading or editing a task artifact, print the active worktree root and `git rev-parse HEAD`; use absolute paths from that worktree and re-check `git status`. |
| 2026-09-21 | A prototype of `IsLeftDerivedFunctor` along the isomorphisms on a category already equivalent to its derived image compiled, but expressed only equivalence transport and added no non-flat/K-projective content. Adversarial review caught the tautology before code was accepted. | Require a concrete non-isomorphism weak equivalence or a proved non-flat derived effect in the theorem's data; reviewers should challenge whether the conclusion follows solely by equivalence transport. |
| 2026-09-22 | A reviewer in a different, stale SF8 worktree recommended replaying the H⁻¹-only chunk even though PR #1457 had merged and the active `origin/main` was exactly `b8306d6b`. | Give every adversary the absolute active worktree and full base SHA; require them to verify `git rev-parse HEAD`, `origin/main`, and live PR state before recommending a chunk. Treat summaries from another checkout as untrusted until reconciled. |
| 2026-09-22 | Mathlib's Tor definition, its chosen-projective-resolution computation, and the chain/cochain indexing bridge are in separate files. `Tor` derives the second tensor factor; using `Tor'` or silently swapping arguments would be wrong. Also, `extendScalars` did not synthesize `Additive` in the probe; fixed-left tensor followed by restriction of scalars did. | Inspect pinned declarations and instantiated types, not filenames alone. State the Tor orientation and target module category explicitly, and test typeclass obligations before writing the public statement. |
| 2026-09-22 | The candidate restriction from `CochainComplex ℤ` to `ChainComplex ℕ` compiled through its terms, augmentation, and projectivity, but `QuasiIso` did not. `restrictionHomologyIso` cannot handle degree zero here because `embeddingDownNat` does not contain the cochain successor `1`; an edge-specific proof is required. | Add a boundary-index case to proof plans before freezing any chain/cochain comparison; do not treat a generic interior-degree lemma as covering degree zero. |
| 2026-09-22 | A query to the issue tracker used a guessed repository owner and failed; resolving the configured remote gave `chris-dare-dev/derived-alg-geo-lean`. | Use the repository slug in the validated loop manifest or inspect the configured remote; never infer a provider owner from a shortened repository name. |
| 2026-09-22 | The worktree-cache helper was not executable by direct invocation, though running it with `bash` worked. | Preserve and report the exact failed invocation; inspect the script mode/shebang and use its documented shell entry point rather than inferring the script itself is broken. |
| 2026-09-22 | A fresh tracked OpenSpec/manifest draft cannot itself pass `preflight`: the controller requires a clean checkout exactly at `base_ref`, but the draft exists only on the implementation branch. The attempted local-tag base workaround was rejected because `action push` does not publish tags and the ledger does not bind the resolved base OID. | Use the repository's one-time, owner-authorized planning-only bootstrap PR pattern (as in SF8 PR #1447); land the plan and enabled manifest on main without code or issue closure, then start implementation from a fresh clean-base checkout and use the controller for all code-phase provider actions. |
| 2026-09-22 | A reviewer tried `python3 scripts/loop_engine.py validate <manifest>`; the CLI accepts the spec only as `validate --spec <manifest>`. This was a usage error, not a manifest failure. | Follow `--help` and the exact command shown in the manifest README; report parser/usage failures separately from validation verdicts. |
| 2026-09-22 | The controller hashes every required OpenSpec artifact, including `tasks.md`, and refuses PR creation or merge if any changes after ledger initialization. Marking task 1.7 complete in its implementation PR would invalidate the evidence needed to create or merge that PR. | Freeze OpenSpec artifacts throughout the code run; leave the task unchecked in the implementation PR and sync its checkbox only after merge in a separate plan-only update. Do not weaken digest validation mid-run. |
| 2026-09-22 | The altitude review found that the live #554 issue checklist and roadmap summary still list a nonidentity, nonflat supported example as outstanding, although merged PR #1404 supplies the affine witness and #1457 adds its explicit H⁻¹ effect. The roadmap gate passes without reconciling this stale prose. | Before eventual issue closure, reconcile the issue checklist and roadmap summary against merged PR evidence in a separately scoped tracker update. Do not reopen an already supplied example as implementation work, and do not infer that the remaining K-flat, preservation, and coherence obligations are complete. |
| 2026-09-22 | Porting the scratch Tor bridge into the source file exposed several API/syntax assumptions: `tensoringLeft` needs `CategoryTheory.MonoidalCategory` opened; the public comparison is an `Iso`, so it must be a `noncomputable def` rather than a Lean `theorem`; and the Tor degree reindexing only elaborated with the literal negative indices passed to `restrictionHomologyIso`. | Preserve the exact scratch imports/open declarations and compiled index expressions when moving a proof into a module. Treat these as API-discovery/transcription failures, not mathematical counterexamples, and build the named source target before review. |
| 2026-09-22 | `python3 scripts/check_audit.py --help` treats `--help` as its required output-file path and fails with `FileNotFoundError`; unlike the adjacent completeness checker, it has no CLI help parser. | Read the audit script's usage/docstring or source signature before probing legacy scripts with conventional help flags; pipe audit output to `/dev/stdin` when checking a single audit leaf. |
| 2026-09-22 | `loop_engine.py validate` accepts an unquoted `#554` inside a YAML acceptance scalar, but YAML treats it and the rest of that scalar as a comment. The initialized ledger therefore ended its last acceptance item at “references,” despite the source line also specifying the no-close and review/CI conditions. The configured progress closure, four reviewers, required `ci`, and disabled issue-close mutation remain separate and intact. | Quote or block-scalar any manifest value containing issue references; inspect `ledger show` after init, and add schema validation that detects comment-truncated acceptance strings rather than relying on a successful YAML parse. |
| 2026-09-22 | The abstraction review found a public nonzero-Tor theorem whose proposition mentioned private module abbreviations. Lean exposed generated inaccessible names in the imported theorem type, although the adjacent comparison theorem already spelled out the concrete modules. | Keep private abbreviations inside proof terms; spell public specialization data in the declaration type and inspect imported `#print` output when reviewing API accessibility. |
| 2026-09-22 | OpenSpec's generated apply guidance and the generic run-loop guidance allow up to five review rounds, while this SF8/SF9 change's design and enabled manifest cap a frozen chunk at three and make the third unsuccessful round terminal. | Treat the task-specific frozen manifest and its three-round budget as the controlling ceiling; generated or generic guidance cannot widen it. Reconcile these sources when the guidance is next maintained. |
| 2026-09-22 | The review ledger becomes terminal immediately after a passing adjudication. A later process-only commit in an allowed file could not receive another recorded review, even though three rounds were configured; the extra note therefore had to stay out of the code PR. | Freeze all code, audit, and friction-log entries before final review and adjudication. Treat `passed` as immutable and route newly discovered documentation work through a separate reviewed plan-sync change. |
| 2026-09-22 | The desktop worktree initially used its generic `codex/` branch prefix, but `loop_engine.py` allows only the manifest-planned `agent/<issue-slug>` branch for provider actions. Detecting this after code review required preserving the reviewed commit on a new planned branch. | Select the manifest's exact issue branch when opening the implementation worktree; validate branch naming before the first commit, not at publication time. |
| 2026-09-22 | `gh pr checks --required` returned “no required checks reported” while the PR workflow jobs were running, even though branch protection requires the aggregate `ci` context. The workflow exposes `build` and `roadmap` first, then runs the `ci` aggregate after both finish. | Inspect the base branch's required contexts and the workflow's aggregate job; monitor the full PR status rollup rather than treating an empty `--required` filter as a CI failure or as proof that no gate is configured. |
| 2026-09-22 | Several read-only probes guessed paths instead of consulting the active tree: `docs/architecture/loop-friction-log.md`, `DerivedAlgGeo/AlgebraicGeometry/Families`, and `scripts/AlgebraicGeometryAudit/Modules.lean` do not exist; the actual locations are `loop-engineering-friction.md`, `AlgebraicGeometry/DerivedCategory/Families/`, and `scripts/AlgebraicGeometryAudit/ModulePullback.lean`. | Resolve filenames with `rg --files` (and use the paths frozen in the manifest) before opening them; do not reconstruct this repository's hierarchy from remembered Mathlib paths. |
| 2026-09-22 | An API probe guessed `Adjunction.leftAdjointUniqIso`; the pinned API uses `Adjunction.leftAdjointUniq`. The wrong identifier interrupted exploration of the affine pullback/tilde comparison, but was not a mathematical obstruction. | Search the pinned source or run a focused `#check` before encoding remembered theorem names; preserve the exact compiled spelling in the probe notes. |
| 2026-09-22 | A broad web search for issue `#554` surfaced unrelated GitHub issues with the same number in other repositories. The configured repository's direct issue URL identified the intended SF8.5 contract. | Resolve issue URLs from the validated `owner/repository` in the active manifest or configured remote; issue numbers alone are not globally identifying. |
| 2026-09-22 | The affine derived pullback API transports extension of scalars through the quasi-coherent equivalence, but that alone does not identify it with full `Scheme.Modules.pullback` on module sheaves. Treating these as interchangeable would silently upgrade an underived representative comparison into a scheme-level derived claim. | Prove and audit the explicit affine pullback/tilde comparison before using the transported affine operation as a geometric pullback; keep `derivedPullback`'s exactness gate and the general K-flat gap visible. |
| 2026-09-22 | Two exploratory shell calls failed before reading or changing files: an unescaped backtick terminated a quoted Bash command, and `rg -E` was mistaken for an extended-regex flag (`-E` means encoding in ripgrep). | Use single-quoted shell patterns or avoid shell quoting around Markdown syntax; use ripgrep's `-e` for an explicit pattern and consult `rg --help` when option spelling is uncertain. |
| 2026-09-22 | At the initial scratch stage, the generic affine pullback/tilde comparison and its lift to arbitrary cochain complexes compiled, but the concrete nonzero `H⁻¹` had not yet been transported into the target scheme-derived category. | Keep the full derived transport as a frozen acceptance condition until one theorem composes the actual pullback comparison with the module-side calculation; do not infer the endpoint from separate scratch lemmas. |
| 2026-09-22 | A follow-up scratch proof compiled the affine realization's target scheme-module `H⁻¹` nonvanishing using `mapDerivedCategoryHomologyIso`, `affineTildeDerivedHomologyIso`, and the affine QCoh inclusion, but at that point it still had not composed the result with the actual-pullback/tilde complex comparison. The source theorem now completes that composition and builds. | Preserve the exact endpoint in the single source theorem and rerun its named target after edits; separate compiled lemmas are not a substitute for the end-to-end check. |
| 2026-09-22 | After the planning bootstrap merged, controller preflight rejected a clean detached checkout at the exact base SHA because `git branch --show-current` is empty. Its diagnostic says “from a detached or dedicated agent branch,” but the current implementation rejects both the protected branch and a detached checkout. Creating the manifest's dedicated local `agent/sf8-5-affine-resolution-pullback` branch at that same commit made preflight pass. | Run from a dedicated local issue branch at the exact base SHA and match the manifest slug. Treat the current diagnostic as stale/misleading; a future controller maintenance change should align the predicate and error text. |
| 2026-09-22 | The end-to-end scratch transport hit two Lean elaboration mistakes: the target homology functor was first inferred on `Scheme.Modules` rather than `DerivedCategory (Scheme.Modules)`, and writing the factorization RHS with a separately inferred `HasDerivedCategory.standard` caused a `Q` universe mismatch. The pieces compile when the target `Q` object is named through `(F.mapHomologicalComplex up ⋙ DerivedCategory.Q).obj baseChangedResolution` and `F.mapDerivedCategoryFactors.app` supplies the comparison. Explicit scheme-module abelianity and affine-tilde exactness imports/instances were also missing in the probe. | Name the target derived-category functor and `Q` object explicitly before applying naturality/isomorphisms, import the module `AB` and quasicoherent extension leaves, then compile the single source theorem as recorded in the next row. |
| 2026-09-22 | Moving the final transport into the source exposed concrete elaboration edges hidden by generic scratch variables: the generic private additive instance did not synthesize for the two concrete tilde functors, `Functor.map_id` resolved to the natural-transformation theorem rather than the objectwise field, and the named `zmodTwoSchemeMap` needed unfolding to match `Spec.map zmodTwoRingMap`. The combined target theorem now compiles after using two explicit private concrete additive instances, `F.map_id`, and unfolding that alias. | Recheck typeclass synthesis at the final concrete specialization; distinguish the functor field `F.map_id X` from the natural-transformation lemma, and unfold named map abbreviations only at the comparison boundary. |
| 2026-09-22 | An early scalar-extension probe used change-of-rings tensor notation without opening the `ChangeOfRings` scope and without the tensor-product tower API import, so parsing/elaboration failed before reaching the intended distributivity proof. | Copy the exact scope and focused tensor-product import from the compiling leaf, and separate parser/import failures from proof failures in the run notes. |
| 2026-09-22 | The first focused `check_audit.py` call supplied only the output path, so the script silently used its default `StabilityConditionAudit.lean` command count and correctly rejected a 29-record ModulePullback audit. The optional source-audit argument is documented in the script docstring but not in the short `CONTRIBUTING.md` example. | For a non-default audit leaf, pass both the captured output and its exact `.lean` source path; verify the audit-specific command count before treating a mismatch as truncation. |
| 2026-09-22 | The `ledger show` subcommand uses `--state`, unlike `validate` and provider actions which use `--spec`; the first inspection command guessed the latter and stopped at argparse. | Read each subcommand's `--help` independently instead of assuming a shared option vocabulary across `loop_engine.py` commands. |

These entries are process evidence, not proof evidence. The concrete affine
Tor comparison is proved, audited, and reviewed in merged PR #1464. The broader
abstract Tor generalization remains unverified in
`generalization-backlog.md`; each remaining mathematical claim still needs its
own compiled evidence and independent review.
